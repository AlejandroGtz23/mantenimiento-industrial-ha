# Required because this app uses AndroidX libraries (CameraX, Room, WorkManager).
android.useAndroidX=true
android.enableJetifier=true

# Keeps Gradle responsive on a student development machine.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
kotlin.code.style=official
